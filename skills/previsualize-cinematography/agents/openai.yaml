interface:
  display_name: "Narrated Fable Previsualization"
  short_description: "Direct natural speech transitions into 16:9 Storyboards"
  default_prompt: "Use $previsualize-cinematography to create one 16:9 storyboard.md that keeps each frame to the fewest story-active characters, declares A/B screen sides, opposed looks, axis line, and camera side for every interaction, makes ECU/CU/MCU dominant, labels each brief wider Shot as a story-required position-change exception followed by a tight return, preserves every speech handoff, and passes the full speech and visual-grammar gates."
