interface:
  display_name: "Forest Animal Education Video"
  short_description: "Produce consistent forest-animal educational films"
  default_prompt: "Use $forest-animal-education-video as a fully repository-local director workflow: never invoke an external production Skill, author the Storyboard and Seedance Prompts locally, generate images only with the bundled Seedream adapter, retain only the minimal authoritative artifacts, and pause before and after each single video attempt without automatic retries."
